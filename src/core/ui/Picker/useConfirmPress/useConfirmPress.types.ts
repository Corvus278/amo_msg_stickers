export type ConfirmPress = {
  /**
   * Первое нажатие сделано и ждёт подтверждения: кнопка показывает текст подтверждения.
   */
  isArmed: boolean;

  /**
   * Нажатие на кнопку. Первое взводит подтверждение и возвращает `false`, повторное в
   * течение 2,5 с возвращает `true` — действие можно выполнять.
   */
  press: () => boolean;
};
