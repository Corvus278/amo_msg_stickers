export type CellSend = {
  /**
   * Идёт отправка из этой ячейки: ячейка полупрозрачна и не нажимается.
   */
  isBusy: boolean;

  /**
   * Отправляет элемент ячейки. Промис не отклоняется: ошибка уходит в строку статуса.
   */
  sendItem: () => Promise<void>;
};
