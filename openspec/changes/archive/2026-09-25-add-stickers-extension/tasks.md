## 1. Каркас проекта

- [x] 1.1 Создать проект на TypeScript и esbuild с `npm run build`, собирающим `dist/extension/*` и `dist/amo-stickers.user.js`; проверка — `npm run build` и `npx tsc --noEmit` проходят без ошибок
- [x] 1.2 Подключить `gifenc` и `lottie-web` (light canvas); проверка — в `dist/extension/content.js` нет `eval(` и `new Function(`

## 2. Ядро: DOM amo и отправка

- [x] 2.1 Модуль селекторов amo: поле ввода, обёртка эмодзи, «Отправить», отмена редактирования; проверка — на стенде `dev/harness.html` находится одно поле ввода
- [x] 2.2 Отправка через `paste` → ожидание вложения (15 с) → клик «Отправить»; проверка — на стенде клик по стикеру добавляет картинку в ленту
- [x] 2.3 Блокировка при непустом черновике и редактировании с текстом ошибки; проверка — на стенде с текстом в поле показывается ошибка и ничего не отправляется
- [ ] 2.4 Проверить отправку на настоящем web.amo.tm (расширение и userscript): стикер доходит до второго аккаунта, в консоли нет ошибок

## 3. Конвертация

- [x] 3.1 GIF-энкодер с 1-битной прозрачностью и `dispose=2`; проверка — прозрачный PNG на стенде даёт GIF с прозрачным фоном
- [x] 3.2 Изображения (ImageDecoder, фолбэк на createImageBitmap), лимиты 512 px, 100 кадров, 4 с; проверка — PNG 512×512 даёт 512×512
- [x] 3.3 Lottie `.tgs` → GIF; проверка — `.tgs` на 60 fps, 3 с даёт GIF из 75 кадров по 40 мс
- [x] 3.4 Видео → GIF покадровым seek
- [ ] 3.5 Проверить конвертацию WebM (VP9 с альфой) в активной вкладке: результат анимирован, фон прозрачный
- [x] 3.6 Подпись на всех кадрах; проверка — на стенде подпись видна в предпросмотре
- [x] 3.7 Качество: палитра rgb565 по непрозрачным пикселям, адаптивный дизеринг Байера, 512 px с понижением при весе больше 2 МБ, 25 fps; проверка — на стенде градиент без полос (ошибка 1,7/255), плоская `.tgs` на 60 fps, 3 с даёт 75 кадров по 40 мс, 512×512, 432 КБ
- [ ] 3.8 Проверить понижение размера на анимации тяжелее 2 МБ в 512 px: результат ≤ 2 МБ и в 384/320/256 px
- [ ] 3.9 Переимпортировать ранее загруженные паки и сравнить качество с Telegram

## 4. Библиотека

- [x] 4.1 IndexedDB: паки, стикеры, недавние (до 40 элементов, без дубликатов); проверка — стикер на стенде сохраняется после перезагрузки
- [x] 4.2 Удаление стикера, недавнего и пака с подтверждением повторным нажатием; «Мои стикеры» не удаляются

## 5. UI

- [x] 5.1 Кнопка после эмодзи с классами amo и активным цветом в открытом состоянии; проверка — на стенде иконка становится `fill-blue-50` / `dark:fill-beige-70`
- [x] 5.2 Попап в Shadow DOM на месте эмодзи-попапа, светлая и тёмная тема; проверка — скриншоты стенда в обеих темах
- [x] 5.3 Вкладки «Недавние», «GIF», паки, «Добавить», «Настройки»; закрытие по повторному клику, Escape и клику вне попапа
- [ ] 5.4 Сверить кнопку и попап на настоящем web.amo.tm со скриншотом нативного эмодзи-попапа: отступы, тень, шрифт, иконка

## 6. Источники

- [x] 6.1 GIPHY: гифки и стикеры, тренды, поиск, подгрузка, атрибуция
- [x] 6.2 KLIPY (формат Tenor v2 на `api.klipy.com`) вместо Tenor, ключ в настройках, host permissions в manifest
- [ ] 6.3 Проверить GIPHY и KLIPY с реальными ключами: тренды, поиск, подгрузка, отправка; проверка — GIF из каждого источника доходит до чата
- [ ] 6.4 Уточнить требования KLIPY к атрибуции и привести подпись под выдачей в соответствие
- [x] 6.5 Импорт из Telegram: разбор ссылки, `getStickerSet` → `getFile` → конвертация, прогресс, устойчивость к сбою одного стикера
- [ ] 6.6 Проверить импорт реального пака со статичными, `.tgs` и видео-стикерами; проверка — счётчик доходит до N/N, все типы анимированы; повторный импорт не создаёт дубликат

## 7. Окружения и установка

- [x] 7.1 MV3: content script, service worker для сети (base64 для бинарных данных), `chrome.storage.local` для настроек
- [x] 7.2 Userscript с `fetch` и `localStorage`, защита от двойного запуска
- [ ] 7.3 Загрузить `dist/extension` в Chrome как распакованное расширение; проверка — в консоли web.amo.tm `[amo-stickers] started (extension)`, кнопка на месте
- [ ] 7.4 Установить userscript в Tampermonkey; проверка — на web.amo.tm кнопка появляется, стикер отправляется

## 8. Риски и приёмка

- [ ] 8.1 Ручная проверка главного риска: отправленный GIF анимирован у получателя во всех клиентах amo. Результат зафиксировать в `design.md`
- [ ] 8.2 E2E на стенде (Playwright): встраивание кнопки, открытие и закрытие попапа, создание стикера, отправка, блокировка при непустом черновике; проверка — `pnpm test` проходит
