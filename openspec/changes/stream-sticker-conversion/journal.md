# Журнал прогона

Решения без заказчика, отступления от спеки, итоги аудитов и долг прогона change `stream-sticker-conversion`.

## 2026-09-26

### Живые образцы

База прогона: 5efdc818a25dbafb346a1a0a7fa68ee8809f5d06.
Образцы: пак video_gachi импортируется через Telegram Bot API на стенде dev/harness.html (токен бота уже в настройках стенда); .tgs/WebP/PNG — из импортируемых паков или синтетика.
Браузерные задачи: агенты делают стенд-части (1.3, 2.1 — стенд, 7.1, 7.2) через chrome MCP. Живой amo (2.1 — живой amo, 7.3, 7.4) — пользователь, задачи остаются [ ] до его сверки; 8.3 (PR, CI) — после этого.
Грязь дерева: сам каталог change (untracked), не чужие правки.

### G1 · Подготовка и базовая линия

1.2: версия 0.4.1 → 0.5.0 в трёх местах правкой package.json: `pnpm version` падает на неотслеживаемом каталоге change.
1.3: базовая линия на 5 стикерах video_gachi в headless Chrome (chrome-devtools MCP): claude-in-chrome прячет вкладку без фокуса, `<video>` падает по таймауту. Вместо UI замерялся прямой вызов toStickerGif, вместо Performance — RSS рендерера и longtask. Итог: long task 1,3–4,7 с на стикер, RSS 143 → 857 МБ, все на 256 px, 3 из 5 > 2 МБ. Бенч в local/bench, образцы в local/samples.
2.1 стенд: blob-Worker и transfer работают, у web.amo.tm CSP нет — в design.md → Risks; живая часть за пользователем, 2.1 остаётся [ ].
Аудит: ok с первого круга.
Долг: CLAUDE.local.md, строка 00 — 76 кадров, по блокам GIF 75; в 7.1/7.2 сверять с 75.

### G2 · Потоковый кодировщик и выбор стороны

3.1/3.2: indexFrame и запись кадра вынесены в gifEncoder.ts (K1); encodeGif работает поверх createGifEncoder, байт-в-байт с базой (эталонные SHA-256 пересчитаны аудитором из 5efdc81). finish() отдаёт копию точной длины — под transfer в G6.
3.3/3.4: sidePick.ts (K2): оценка с Math.ceil, ровно 2 МБ укладывается; sampleCount=0 → сторона источника; тест «тяжёлая анимация» проверяет ровно 384, чтобы ловить линейную зависимость.
Отступление: G2 дописала `bytesView()` в объявление gifenc в src/types.d.ts (файл G6) — аддитивно, иначе byteLength копировал бы поток на каждый кадр.
Аудит: ok с первого круга.
Долг: convert.ts:30 FALLBACK_SIZES дублирует FALLBACK_SIDES из sidePick — G4 обязана брать sideLadder (проверить на аудите G4); sidePick.test.ts:60 первая проверка «пересчитывает пробу» дублирует «лимит недостижим».

### G3 · Источники кадров

5.1–5.4 + диспетчер openFrameSource (K3, switch с never): frameSourceCommon (waitForEvent через AbortController, fit, planItem), frameSourceVideo, frameSourceTgs (холст fit(json.w,json.h,maxSide)), frameSourceImage (план ImageDecoder с немедленным close каждого кадра). draw сам стирает холст.
Решение координатора: план ImageDecoder обрезается до 4 с по букве спеки (spec.md:4), хотя design D1 велел правила базы; кадр, начинающийся на 4 с и позже, не идёт. Альтернатива — оставить только предел 100 кадров, как в базе.
Отступления от базы (D1): позиция кадра index*step без накопления — у видео 0,40–0,56 с и .tgs 30/60 fps на границе на кадр меньше (лишний кадр за концом ушёл); одиночный кадр ImageDecoder (PNG) — задержка 0 вместо 100 мс, по задаче 5.4.
Аудит: ok с первого круга. question (к пользователю): у картинок последний кадр не урезается — при неравных кадрах длительность может заметно превысить 4 с (35×100 мс + 2000 мс = 5,5 с); вариант A — как сейчас, B — урезать последнюю задержку до max(20, 4000 − start).
Долг: frameSourceTgs.ts:100–104 — мёртвый try/catch вокруг lottieLoaded (промис не отклоняется). Дубли констант convert.ts удаляет G4.

### G4 · Оркестрация и фолбэк: первый сквозной срез

4.4/6.1: лестница — чистая encodeLadder({frameCount, side, sample, encode}) только через sideLadder/pickSide/MAX_GIF_BYTES из sidePick; encode(side) не видит прошлых кадров — каждый проход по источнику. Фолбэк — frameSinkMain.ts, frameSink.ts пока перенаправляет в него (G6 добавит Worker, не трогая фолбэк). Проба рисует подпись. Из convert.ts удалены FALLBACK_SIZES, fit, константы плана, Frame, encodeGif (не использовались).
Стенд (headless Chrome, local/bench/g4.js): video_gachi 00/12/20/30/44/03 — стороны и вес как в базе, 0,4–1,7 с против 1,0–5,4 с, long task > 50 мс нет (база 0,9–4,7 с), пик heap 18–27 МБ; мусорный/обрезанный .webm — ошибка за 15/31 мс, native <video> 0; .tgs 60 fps/3 с → 75×40 мс; WebP → 75×40 мс; PNG 2560×1600 → 512×320, задержка 0.
6.2: guard-плагин vitest (временный) — тесты не грузят convert.ts и frameSink.ts; повторить в G6, когда появится gif-worker:code (K5).
Отступление: tests/frameSinkMain.test.ts вне списка файлов G4 — тест модуля самой группы.
Аудит: ok с первого круга.
Риск (долг G6/G7): setTimeout(0) в скрытой вкладке Chrome тормозится до 1 Гц — фолбэк на 75 кадрах > 1 мин; стенд запущен с --disable-background-timer-throttling и этого не видит.

### G5 · Сверка на живых образцах video_gachi

7.2: весь video_gachi (45) на стенде, ошибок 0. SAFETY остаётся 1,1: 44/45 — один проход, второй у № 31 (320 px: оценка 1937 КБ, факт 2050 КБ). Альтернатива 1,17–1,18 держится на двух стикерах, с 1,19 № 10 теряет 320 px. Факт/оценка растёт с уменьшением стороны (512 — 1,00 … 256 — 1,21) — предложение про показатель степени < 2 записано в design.md → Open Questions.
Итог: longtask ≤ 218 мс (база 858–4702 мс); стороны 256 — 41, 320 — 2, 384 — 2; > 2 МБ при 256 px — 18/45. Пик кучи после gc() 38–42 МБ, одинаков для одного прохода и лестницы (мерилось принудительными проходами __sides в ladderLog.ts, не повтором encodeLadder).
Долг G1 закрыт: у 00 в базовой линии 75 кадров.
Аудит: ok с первого круга.
Долг: design.md:203 и CLAUDE.local.md — «RSS 790–815 МБ» по логу g5-rss2.log верно «772–815 МБ без прогревочного окна (672 МБ)» — поправить в G7.

### G6 · Worker из blob URL

4.1/4.2: gifWorker.ts — чистый обработчик протокола (start/frame/ack/finish/done/error, новый start = новый GIF), gifWorkerEntry.ts — точка входа; плагин build.mjs вшивает код Worker-а строкой (gif-worker:code) в content.js и userscript, не в background.js; eval/new Function в dist — 0.
4.3: workerSinkClient.ts — чистый клиент (кадр до первого ack уходит копией и повторяется в фолбэке), createFallbackAwareSink с флагом страницы в замыкании; workerSink.ts только подставляет Worker из blob URL (отзывается сразу после new Worker). Фолбэк — по error Worker-а и исключению конструктора; сообщение error/messageerror — ошибка конвертации. Стенд: Worker 0,42–1,65 с, long task 0, куча главного потока 5–9 МБ; SHA-256 GIF Worker-а и фолбэка совпадают. 6.2 повторена guard-плагином: тесты не грузят gif-worker:code (K5).
Решения координатора (спека молчит): окно 1 кадр вместо 2 (D4, текст 4.3) — samplePass читает byteLength сразу после последнего write, при окне 2 проба теряла бы последний кадр (−12,5 % на 8 кадрах, больше SAFETY); окно 2 быстрее ~30 %, требует flush пробы в convert.ts. Worker на проход, а не на вызов toStickerGif (D3/D8) — K4 запрещал править convert.ts, заодно без утечки между стикерами.
Отступление: tests/gifWorker.test.ts, gifWorkerEntry.ts, workerSinkClient.ts вне явного списка файлов — модули самой группы.
Аудит: круг 1 — critical (флаг isWorkerBroken без теста) + долг finishWaiter; круг 2 — ok.
Долг: tests/gifWorker.test.ts:87 («пробный и полный проход в одном Worker-е») описывает режим, которого в продукте нет; нет таймаута на зависший Worker (не специфицирован).

### G7 · Приёмка на стенде и документация

7.1: стенд, сырые данные local/bench/g7-run.json (12 кейсов: WebP/PNG попиксельно как база, видео ΔRGB ≤ 1,66, альфа 0; .tgs альфа 0,07 %) и g7-pack.json (video_gachi 45/45 по пути Worker-а, вызов toStickerGif как в telegram.ts, не UI-импорт: ошибок/фолбэков 0, 44 одинарных прохода, 91 Worker, long task 0, пик кучи 10,5–15,9 МБ без роста). Свои стикеры с подписью — через UI стенда; прозрачность — на синтетике anim-alpha.webp/alpha.png.
8.1: CLAUDE.md — конвейер, выбор стороны (два условия пробы), Worker из blob URL, окно 1, фолбэк и флаг страницы, замедление фолбэка в скрытой вкладке, vi.mock для транзитивного convert (K5), дерево «Структура». Долг G5 (RSS 772–815 МБ) закрыт в design.md.
8.2: pnpm lint/test (204)/build — без ошибок и предупреждений.
Аудит: круг 1 — critical (нет сырых данных 7.1 и прогона пака); круг 2 — ok.
question (к пользователю): время конвертации на пути Worker-а в g7 — 3,7–7,5 с на 00/12/20/30/44 и медиана 6,6 с по паку (№ 37 — 41,6 с), против 0,42–1,65 с в G6 и 1,0–5,4 с в базе; спека время не описывает. Причина (окно 1 или нагрузка машины) не различена.
Долг: CLAUDE.md «Фолбэк» — «поэтому основной путь — Worker» натянуто: Worker основной, потому что снимает кодирование с главного потока.

### Диагност: время конвертации G7

Question аудитора G7 (время на пути Worker-а) снят координатором без пользователя: регрессии нет, G7 мерился на нагруженной машине.
Замер на свободной машине (load 1,8–2,1 / 10 ядер, 3 раунда, разброс < 3 %), стикеры 00/12/20/30/44: база 1,67–5,11 с (сумма 16,3–16,9, long task до 39,6 с); Worker окно 1 — 0,70–1,69 (6,1–6,2, long task 0); окно 2 — 0,51–1,12 (4,2–4,3); фолбэк — 0,72–1,66 (6,0–6,1, long task 199 мс). № 37 (23,7 fps): база 39–40 с, окно 1 — 9,0–9,2 с. Пак на бандле G7: медиана 1,18 с, сумма 73 с, макс 9,2 с (в G7 было 6,6 / 369 / 41,6 — равномерно ×3,6–5,6, признак нехватки CPU). g6-w1/g6-w2 из HEAD байт-в-байт с бандлами G6.
Решение: окно 1 остаётся (решение G6). Окно 2 (−32 %, нужен flush пробы в convert.ts) и скорость захвата видео с дробным fps — кандидаты в отдельные задачи, спека время не требует. Цифры для PR (8.3) брать из этого замера, а не из g7-pack.json.

### F1: обрезка картинок до 4 с

Ответ пользователя на question G3/итога: вариант B. Критерий F1: план ImageDecoder-источника — задержка последнего кадра урезается до max(20, 4000 − startMs), суммарная длительность GIF ≤ 4000 мс + 20 мс; тест с зубами на неравных кадрах (35×100 мс + 2000 мс → итог ≤ 4020 мс).
Попутно (долг итогового аудита): в журнале/CLAUDE.md фолбэк в скрытой вкладке — после 5 мин Chrome включает intensive throttling (~1 раз в минуту), а не 1 Гц; обоснование «поэтому основной путь — Worker» в CLAUDE.md переписать (Worker снимает кодирование с главного потока).

### F1

F1 по ответу пользователя (вариант B): в плане ImageDecoder задержка кадра = min(frameDelayMs, max(20, 4000 − startMs)) — урезается только кадр, выходящий за 4 с; итог ≤ 4000 + 20 мс. Тесты: 35×100 + 2000 → 500 мс (итог 4000), 3990 + 100 → 20.
CLAUDE.md: «Анимация» — урезание последнего кадра; «Фолбэк» — Worker основной, т. к. снимает кодирование с главного потока; скрытая вкладка — intensive throttling через 5 мин (~1 раз в минуту; «1 Гц» в записи G4 неточен).
Аудит: итоговый аудитор в режиме «группа» + перепроверка своего question и долга — ok с первого круга (отдельный перепроверочный заход не нужен).

### Итог

Гейт `pnpm lint && pnpm test && pnpm build` — ok (19 файлов, 206+ тестов); openspec validate --strict — ok. Итоговый аудит — ok, покрытие: 2 требования, 11 сценариев, без проверки 0.
Закрыто 21/25. Открыты (живой amo, пользователь): 2.1 (живая часть — Worker из content script и Tampermonkey), 7.3 (импорт video_gachi, расширение, база 5efdc81 снимается в живом amo), 7.4 (Tampermonkey, анимация у получателя); 8.3 (PR, Closes #8) — после них, цифры — из записи «Диагност: время конвертации G7».
Вопросы: A/B обрезки картинок — B, сделано в F1. Открытых нет.
Кандидаты в отдельные задачи: окно Worker-а 2 кадра (−32 %, flush пробы в convert.ts); захват видео с дробным fps (№ 37); таймаут зависшего Worker-а; точнее оценка веса (показатель степени < 2).

### Версия 0.6.0

origin/master уже на 0.5.0 (PR #14), а G1 сверял версию с устаревшим локальным master (0.4.1). Ветка перебазирована на свежий master (e8a5d5f), версия поднята до 0.6.0 в package.json, manifest.json и @version в build.mjs; `check-version.mjs --base origin/master` — ok, гейт — ok. Текст задачи 1.2 («0.5.0») устарел — правка артефакта через /opsx:update при желании.

### 7.4: userscript и CORS Telegram

Живой amo, Tampermonkey: импорт пака падает на CORS при скачивании файла `api.telegram.org/file/bot…` (нет Access-Control-Allow-Origin). Ошибка не этой задачи: сеть userscript в ветке не менялась, прямой fetch под CORS страницы — известное устройство цели. Заведён issue #15 (bug).
Решение: 7.4 проверяется своим стикером из .webm (local/samples/video_gachi) — тот же toStickerGif → Worker → отправка; импорт целого пака проверяется в 7.3 через расширение. Критерий 7.4 «импорт дошёл до конца» в userscript недостижим до #15 — при желании поправить текст задачи через /opsx:update.

### Живой-amo

Сверка пользователя в живом amo (2026-09-26): расширение и userscript в Tampermonkey работают, импорт быстрее, вкладка по памяти не падает. Записано в design.md → Risks (спайк 2.1, живой amo): Worker из blob URL — основной путь в обеих целях.
Отступления: 7.3 — цифр Performance/памяти в живом amo не снималось, сравнение «до/после» для PR берётся из замера диагноста на стенде (запись «Диагност: время конвертации G7»); 7.4 — проверено своим стикером, импорт пака в userscript блокирует CORS (issue #15).
Приёмка — пользователь, аудита агентом нет (живой amo агентам недоступен).

### PR

8.3: PR #16 в master с Closes #8, замеры до/после из записи диагноста. Версия 0.6.0 (origin/master уже 0.5.0 после #14).
Аудит review-staged (ветка против master): 18 находок (2 P1, 16 P2) — все поправлены и подтверждены повторным аудитом; повторный раунд дал 2 новые P2 (N1 — тест таймаута lottieLoaded, N2 — type вместо JSON.stringify в workerSinkClient) — поправлены и подтверждены. 20 тредов резолвлены.
CI typecheck падал: тесты импортировали node:events/node:crypto, @types/node в проекте нет (локально находился в ~/node_modules). Поправлено 275813d: Web Crypto и хелпер CountingEventTarget.
CI на 275813d: lint, typecheck, test, build, version — зелёные.
