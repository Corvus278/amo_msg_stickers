## 1. Подготовка

- [x] 1.1 Ветка `fix/8-video-import-memory` от свежего `master` под issue #8; проверка: `git log master..HEAD` пуст
  до первых правок
- [x] 1.2 Поднять версию до `0.5.0` в `package.json` (`pnpm version 0.5.0 --no-git-tag-version`),
  `src/extension/manifest.json` и `@version` в `build.mjs`; проверка: `node scripts/check-version.mjs --base master`
  — код 0
- [x] 1.3 Снять базовую линию на стенде `dev/harness.html` до правок: импорт 3–5 стикеров `video_gachi` — пик памяти
  вкладки, самая длинная задача главного потока (DevTools Performance / `PerformanceObserver('longtask')`), вес и
  сторона каждого GIF; проверка: цифры записаны в описание PR-черновика или `CLAUDE.local.md` для сравнения в 7.x

## 2. Спайк: Worker из blob URL

- [ ] 2.1 Проверить на стенде и в живом amo, что `new Worker(URL.createObjectURL(new Blob([code])))` с
  `postMessage` и transfer `ArrayBuffer` работает из content script расширения (изолированный мир) и из userscript
  в Tampermonkey; временный код не коммитится; проверка: результат (работает / ошибка с текстом) записан в
  `design.md`, раздел Risks — если Worker недоступен в одной из целей, фолбэк из группы 4 становится для неё
  основным путём и это отмечено там же

## 3. Потоковый кодировщик и выбор стороны

- [x] 3.1 Вынести `indexFrame` и запись кадра из `convert.ts` в чистый модуль потокового кодировщика
  (`{ width, height, isAnimated }` → `write(rgba, delay)`, `byteLength`, `finish()`), без DOM; типы — в соседнем
  `*.types.ts`; проверка: `pnpm typecheck` и eslint по файлу чистые
- [x] 3.2 Тесты кодировщика в `tests/`: однокадровый и многокадровый GIF проходят `inspectGif`, число кадров и
  задержки совпадают с записанными, прозрачный пиксель остаётся прозрачным (индекс прозрачности), у анимации
  disposal «очистить до фона», `byteLength` растёт с каждым кадром; проверка: `pnpm test` зелёный
- [x] 3.3 Чистая функция выбора стороны по `(sampleBytes, sampleCount, frameCount, side)` с константами
  `SAMPLE_FRAMES = 8`, `SAFETY = 1.1` и рядом «сторона источника, затем меньшие неё из 384 / 320 / 256»; и функция выбора индексов пробных кадров
  (равномерно по плану); проверка: `pnpm typecheck` чистый
- [x] 3.4 Тесты выбора стороны: оценка укладывается в 512 → 512; не укладывается ни в одну → 256; источник 300 px →
  ряд `[300, 256]`, без увеличения; граница ровно 2 МБ; индексы пробных кадров уникальны, в
  пределах плана, первый и последний кадр покрыты; проверка: `pnpm test` зелёный, тест падает при замене `(s /
  side)²` на линейную зависимость (временно — откатить)

## 4. Приёмник кадров: Worker и фолбэк

- [ ] 4.1 Точка входа Worker-а: протокол `start` / `frame` (transfer) / `ack { byteLength }` / `finish` / `done`
  (transfer) / `error` поверх кодировщика из 3.1; типы сообщений — в `*.types.ts`; проверка: `pnpm typecheck`
  чистый
- [ ] 4.2 Плагин в `build.mjs`: виртуальный модуль с именованным экспортом кода Worker-а (отдельный
  `esbuild.build` с `write: false`, IIFE, `chrome120`, минификация вне `watch`, входы Worker-а — в `watchFiles`);
  описание модуля в `src/types.d.ts`; проверка: `pnpm build` проходит, в `dist/extension/content.js` и
  `dist/amo-stickers.user.js` есть код Worker-а строкой, `rg -c 'eval\(|new Function\(' dist` — 0 совпадений;
  в `pnpm watch` правка точки входа Worker-а пересобирает `content.js`
- [ ] 4.3 Клиент Worker-а: запуск из blob URL (URL отзывается после старта), окно в 2 кадра с ожиданием `ack`,
  `finish` → байты, `terminate` при закрытии; ошибка до первого `ack` или исключение конструктора → фолбэк и флаг на
  время жизни страницы; ошибка после первого `ack` → ошибка конвертации; проверка: на стенде в DevTools Memory
  при импорте нет роста очереди сообщений (число живых `ArrayBuffer` не растёт с числом кадров)
- [ ] 4.4 Фолбэк на главном потоке: тот же кодировщик, `setTimeout(0)` после каждого кадра, тот же интерфейс
  `write` / `finish`; проверка: на стенде с принудительно выключенным Worker-ом (временный флаг, не коммитится)
  стикер `video_gachi` конвертируется, ни одна long task не длиннее ~1 с

## 5. Источники кадров

- [x] 5.1 Ожидание события элемента: снимает слушатель при срабатывании, таймауте и `error`, отклоняется по `error`
  сразу; проверка: `pnpm typecheck` чистый, на стенде битый `.webm` падает ошибкой быстрее 10 с
- [x] 5.2 Видео-источник: план кадров (25 fps, 4 с, 100 кадров, дотяжка `duration=Infinity` seek-ом в конец),
  `draw` = seek + `drawImage`, `dispose` = `removeAttribute('src')` + `load()` + `revokeObjectURL` при любом исходе;
  проверка: на стенде после ошибки и после таймаута `loadeddata` в DevTools Memory нет живых `HTMLVideoElement`
  от конвертации
- [x] 5.3 Источник `.tgs`: анимация на своём холсте в размере источника, `draw` = `goToAndStop` + `drawImage` в
  целевой холст, план с шагом от исходных fps и задержкой от реального шага; `dispose` = `anim.destroy()`;
  проверка: `.tgs` на 60 fps / 3 с даёт 75 кадров по 40 мс (проверить `inspectGif` на результате в консоли стенда)
- [x] 5.4 Источник картинки: `ImageDecoder` (план по `frameCount` и `duration` кадров, `draw` = `decode` +
  `drawImage` + `close`) и фолбэк `createImageBitmap` (один кадр, битмап до `dispose`); проверка: анимированный WebP
  и статичный PNG конвертируются на стенде, у PNG задержка 0 и один кадр

## 6. Оркестрация в `convert.ts`

- [ ] 6.1 Переписать `toStickerGif`: passthrough GIF без изменений; открытие источника; пробный проход (если кадров
  больше 8 и сторона больше 256); выбор стороны; полный проход; лестница по итоговому весу с проходом по источнику;
  подпись на целевом холсте после масштабирования; `finally` — `dispose` источника и закрытие приёмника; удалить
  `FrameSet`, `scaleFrames`, `encodeWithinBudget` и неиспользуемые типы; проверка: `pnpm lint` чистый, публичные
  `toStickerGif` / `detectKind` / `captionDecorator` / `STICKER_SIZE` не поменяли сигнатуры (`telegram.ts` и
  `useStickerDraft` не правились)
- [ ] 6.2 Проверить, что юнит-тесты не тянут виртуальный модуль Worker-а: `tests/telegram.test.ts` и новые тесты
  запускаются без плагина `build.mjs`; проверка: `pnpm test` зелёный

## 7. Проверка на стенде и в живом amo

- [ ] 7.1 Стенд: свой стикер из видео с подписью, из `.tgs`, из анимированного WebP и из PNG — вид (прозрачность,
  подпись, отсутствие следов кадров) как до правок; проверка: визуально сверено с GIF базовой линии из 1.3
- [ ] 7.2 Замер оценки на `video_gachi`: для каждого стикера — оценка, фактический вес, число полных проходов;
  при систематической ошибке подобрать `SAFETY`; проверка: у большинства стикеров один полный проход, результат
  записан в `design.md` (Open Questions → ответ)
- [ ] 7.3 Живой amo, расширение: импорт `video_gachi` целиком — вкладка не падает, пик памяти не растёт от стикера к
  стикеру, самая длинная задача главного потока ≤ ~1 с (Performance), страница отвечает на ввод во время импорта;
  проверка: цифры сравнены с базовой линией 1.3 и приложены к PR
- [ ] 7.4 Живой amo, userscript в Tampermonkey: тот же импорт `video_gachi`; проверка: импорт дошёл до конца,
  стикер отправляется и анимирован у получателя

## 8. Документация и PR

- [ ] 8.1 Обновить `CLAUDE.md`: раздел «Конвертация» (потоковый конвейер, выбор стороны по пробным кадрам, Worker из
  blob URL и фолбэк, освобождение `<video>`), «Сборка CSS»/«Стек и сборка» (плагин Worker-а), дерево «Структура»
  (новые модули); проверка: описанное совпадает с кодом, `pnpm format` не меняет файл
- [ ] 8.2 Финальная проверка: `pnpm lint` и `pnpm test` зелёные, `pnpm build` проходит; проверка: вывод команд без
  ошибок и предупреждений в новом коде
- [ ] 8.3 PR в `master` с `Closes #8`, в теле — замеры до/после из 1.3 и 7.3; проверка: CI зелёный (`lint`,
  `typecheck`, `test`, `build`, `version`)
