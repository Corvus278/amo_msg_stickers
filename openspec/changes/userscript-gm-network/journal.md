# Журнал прогона

Решения без заказчика, отступления от спеки, итоги аудитов и долг прогона change `userscript-gm-network`.

## 2026-09-26

### Старт прогона

База прогона: 857d969eef5e26666d4d2ae2d02bb420a0bb5d04. Ветка fix/15-userscript-gm-network создана координатором от свежего master (1.1: git log master..HEAD пуст).
Масштаб: средний (20 задач), режим «дерево», последовательно.
Гейт: pnpm lint && pnpm test (+ pnpm build для сборки userscript).
Живые образцы: local/samples/video_gachi/NN.webm (45 файлов пака, set.json) — для офлайн-проверок конвертации; сеть Bot API — только в живом amo.
Задачи вне агентов: 7.3, 7.4, 7.5 — живой amo с Tampermonkey/Violentmonkey в браузере пользователя; в группы не идут, остаются [ ] до проверки пользователем.

### План

Три группы, три волны: G1 (1.1 1.2 2.1–2.4), G2 (3.1–3.4), G3 (4.1 4.2 5.1 5.2 6.1 7.1 7.2). Вне групп — 7.3–7.5 (пользователь).
Решение без заказчика: «пустое хранилище менеджера» = GM_getValue вернул undefined (альтернатива — пустой объект/строка; выбрано буквальное отсутствие значения как надёжнейшее).
7.2: без ключа GIPHY на стенде исполнитель помечает проверку вкладки GIF в отчёте.

### G1 · Версия, контракты ядра, хелперы net, граница eslint

Версия 0.7.0 в package.json правкой строки: pnpm version отказал из-за неотслеживаемого каталога change.
Host = { name } & HostNetwork & HostSettings; хелперы net экспортируются, fetchChecked бросает через httpError.
Граница ядра — no-restricted-imports с regex ^(\.\./)+(userscript|extension)(/|$), импорты типов тоже; абзац в CLAUDE.md «Линтинг».
Зубы 2.3 — мутации net.ts (лимит обрезки, обход httpError) → красные.
Аудит: ok с первого круга.
Долг: eslint.config.mjs:212 — regex не резолвит путь: из src/core/ui/** ловит и ../extension внутри ядра (ложное срабатывание, таких каталогов нет); не-относительные импорты не ловит (алиасов нет).
