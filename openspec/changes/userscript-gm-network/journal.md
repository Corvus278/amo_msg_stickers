# Журнал прогона

Решения без заказчика, отступления от спеки, итоги аудитов и долг прогона change `userscript-gm-network`.

## 2026-09-26

### Старт прогона

База прогона: 857d969eef5e26666d4d2ae2d02bb420a0bb5d04. Ветка fix/15-userscript-gm-network создана координатором от свежего master (1.1: git log master..HEAD пуст).
Масштаб: средний (20 задач), режим «дерево», последовательно.
Гейт: pnpm lint && pnpm test (+ pnpm build для сборки userscript).
Живые образцы: local/samples/video_gachi/NN.webm (45 файлов пака, set.json) — для офлайн-проверок конвертации; сеть Bot API — только в живом amo.
Задачи вне агентов: 7.3, 7.4, 7.5 — живой amo с Tampermonkey/Violentmonkey в браузере пользователя; в группы не идут, остаются [ ] до проверки пользователем.

### План

Три группы, три волны: G1 (1.1 1.2 2.1–2.4), G2 (3.1–3.4), G3 (4.1 4.2 5.1 5.2 6.1 7.1 7.2). Вне групп — 7.3–7.5 (пользователь).
Решение без заказчика: «пустое хранилище менеджера» = GM_getValue вернул undefined (альтернатива — пустой объект/строка; выбрано буквальное отсутствие значения как надёжнейшее).
7.2: без ключа GIPHY на стенде исполнитель помечает проверку вкладки GIF в отчёте.

### G1 · Версия, контракты ядра, хелперы net, граница eslint

Версия 0.7.0 в package.json правкой строки: pnpm version отказал из-за неотслеживаемого каталога change.
Host = { name } & HostNetwork & HostSettings; хелперы net экспортируются, fetchChecked бросает через httpError.
Граница ядра — no-restricted-imports с regex ^(\.\./)+(userscript|extension)(/|$), импорты типов тоже; абзац в CLAUDE.md «Линтинг».
Зубы 2.3 — мутации net.ts (лимит обрезки, обход httpError) → красные.
Аудит: ok с первого круга.
Долг: eslint.config.mjs:212 — regex не резолвит путь: из src/core/ui/** ловит и ../extension внутри ядра (ложное срабатывание, таких каталогов нет); не-относительные импорты не ловит (алиасов нет).

### G2 · Адаптеры сети userscript: GM API и fetch

gmNetwork: readyState в GmResponse (HEADERS_RECEIVED); isArrayBuffer по форме (другой realm); abort при любом отказе, отложенный при синхронном колбэке, isSettled до abort.
Тексты сетевых ошибок: «Сетевая ошибка», «Сервер не ответил вовремя», «Запрос прерван» — без адреса.
Зубы 3.3: 16 мутаций gmNetwork.ts — все красные, файл восстановлен побайтно.
Аудит: ok с первого круга.
Вопрос пользователю: не-2xx с телом больше лимита — «Файл больше» (Content-Length/onprogress) или HTTP <код> (только onload): текст зависит от колбэков менеджера; спека молчит.
Долг: gmNetwork.ts:201 — тело не ArrayBuffer → молча пустой Blob вместо ошибки; gm.types.ts:139 — рантайм-гард в *.types.ts.

### G3 · Настройки, точка входа, заголовок, документация

Настройки: pickSettings пропускает только известные строковые поля; битый/не-объект JSON в storage удаляется без переноса; SETTINGS_KEY экспортируется из settings.ts. Отступление: settings.types.ts вне плана (правило typescript.md).
Точка входа: GM-настройки при наличии и GM_getValue, и GM_setValue (задача называла только GM_getValue — без записи setSettings упал бы); GM_* объявлены `T | undefined`.
Зубы 4.x: 8 мутаций settings.ts — красные. 7.2: стенд в headless Chrome, фиктивные ключи — localStorage, fetch, HTTP 401 в статусе; выдача GIPHY с настоящим ключом не проверена (ключ в браузер не пропустил классификатор) — за 7.3.
Аудит: ok с первого круга.
Долг: index.ts:37 pickSettings — тёзка фильтра в settings.ts:26 (лучше pickHostSettings); CLAUDE.md «Структура» не упоминает settings.types.ts; settings.ts:73 — null из GM_getValue не считается пустым (перенос молча не сработает).

### Ответы пользователя, группа F1

Итоговый аудит: ok, покрытие 12 сценариев (8 автоматически/стенд, 4 ждут 7.3–7.5). openspec validate --strict ok.
Вопрос G2 решён пользователем: для не-2xx приоритетна HTTP-ошибка. Критерий F1-a: gmNetwork при любом не-2xx отдаёт httpError(status, …) независимо от набора колбэков менеджера (HEADERS_RECEIVED/onprogress/onload); большое тело не дочитывается (abort), текст — что доступно, ≤200 символов.
Долг в F1 по выбору пользователя: (b) settings.ts:73 — null из GM_getValue считается пустым; (c) gmNetwork.ts:201 — тело не ArrayBuffer → ошибка вместо пустого Blob; (d) index.ts pickSettings → pickHostSettings + settings.types.ts в CLAUDE.md «Структура»; (e) юнит-тест выбора режима по typeof GM_* (index.ts:30-42).
Ограничение (итог, не правится): с @sandbox DOM флаг window.__amoStickers в изолированном мире — менеджер + прямое подключение дадут два экземпляра.

### F1

F1-a: не-2xx → httpError во всех колбэках (статус из ответа, при 0 — с HEADERS_RECEIVED), тело не дочитывается. F1-b null = пусто; F1-c BODY_NOT_BYTES; F1-d pickHostSettings + settings.types.ts в CLAUDE.md; F1-e tests/userscriptEntry.test.ts на настоящей точке входа (vi.stubGlobal, ядро под vi.mock).
Зубы: 5 новых тестов красные до правок, 10 мутаций исполнителя и 3 аудитора — красные.
Аудит: ok с первого круга. LSP «BODY_NOT_BYTES не найден» — устаревший кэш, pnpm typecheck ok.
Граница F1-a: статус не сообщён ни на заголовках, ни в onprogress, тело сверх лимита → «Файл больше» (иначе лимит не держится).
Долг: gmNetwork.ts:76 bodyText декодирует тело целиком ради 200 символов; текст «HTTP 404 » с хвостовым пробелом при обрыве на заголовках.

### Итог

Гейт pnpm lint && pnpm test && pnpm build — ok (22 файла, 254 теста). openspec validate --strict — ok.
Итоговый аудит ok, перепроверка F1 ok. Покрытие: 3 требования, 12 сценариев; 8 — тесты/стенд, 4 ждут 7.3–7.5.
Закрыто 17/20. Открыты 7.3, 7.4, 7.5 — ручная проверка пользователя в живом amo (Tampermonkey, Violentmonkey); по 7.4 и 7.5 — запись в design.md, Risks.
Вопросы: нет. Ограничение: менеджер + прямое подключение → два экземпляра (флаг в изолированном мире).

### Ручная

7.3 (пользователь): Tampermonkey в Chrome, 0.7.0 поверх 0.6.0 — ключи на месте, импорт video_gachi без ошибок CORS, стикер и GIF GIPHY отправлены.
7.4 (пользователь): typeof GM_xmlhttpRequest / GM_getValue на странице — undefined; Worker стартует, фолбэка нет — записано в design.md, Risks.
7.5 (Violentmonkey) — не проверена, остаётся [ ].
